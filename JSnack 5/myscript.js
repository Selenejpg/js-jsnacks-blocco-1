//Crea un array vuoto.
let array = [];
let boh = "";

//Chiedi per 6 volte all’utente di inserire un numero,
for (let i = 0; i < 6; i++) {
    let boh = prompt("Inserisci un numero"); 
}

//Se è dispari inseriscilo nell’array
if (boh % 2 == 0) {
    
}else {
    array.push(boh);
}